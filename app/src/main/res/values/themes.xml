<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SevenWindsCoffee" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Status and Navigation bar color. -->
        <item name="android:statusBarColor">@color/brown_4</item>
        <item name="android:navigationBarColor">@color/brown_4</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <style name="Theme.SevenWindsCoffee" parent="Base.Theme.SevenWindsCoffee" />
</resources>